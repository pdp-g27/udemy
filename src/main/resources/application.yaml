server:
  port: 8081
  servlet:
    context-path: /api/v1
spring:
  datasource:
    port: 9000
    url: jdbc:postgresql://localhost:${spring.datasource.port}/postgres
    username: postgres
    password: postgres
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
